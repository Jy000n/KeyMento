# KeyMento Hardware Modules

This folder contains code that runs on external hardware.

- `pi_display/`: Raspberry Pi 3B HDMI LCD wireless display module.

The PC runs `hw/pi_display/pc_sender.py`. The Raspberry Pi runs
`hw/pi_display/pi_receiver.py`.
